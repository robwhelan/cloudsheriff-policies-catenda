policies:
  - name: asg-invalid-configuration
    description: invalid autoscaling group config references stale or missing resources.
    resource: asg
    filters:
      - invalid
    actions: #&default-actions
      - type: invoke-lambda
        function: cloud-sheriff-notification
